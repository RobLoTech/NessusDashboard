# ===== NessusDashboard local config (example) =====
# Copy to .env and fill in your values.

# Database (compose service name is typically "db")
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/nessusdashboard

# Optional: CISA KEV sync (if your app uses it via env)
# KEV_URL=https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json
